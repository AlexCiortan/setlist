# Documents the secrets this project needs. Names and comments only; values stay
# empty here and real values live only in the untracked .env (transcript-secrets
# rule: a secret pasted anywhere visible is burned).
#
# EXAMPLE_API_KEY=
