# Design record index

The design source-of-truth index (framework Part 5c): what is locked, what each
redline governs, and which spec consumed it. A design decision that is not
committed here does not exist. Every design intake ends by adding its bundle
(redline documents, mock exports) to this directory and its rows to this table.

| Deliverable | Governs | Locked on | Consumed by spec |
|---|---|---|---|

(No entries yet; the first design intake adds its bundle.)
